apiVersion: v1
kind: Service
metadata:
  name: {{ include "ironsworn_companion.fullname" . }}
  labels:
    {{- include "ironsworn_companion.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ironsworn_companion.selectorLabels" . | nindent 4 }}
